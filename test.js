function solution(answers){
    const a='12345'.split('')
    const b='21232425'.split('')
    const c='3311224455'.split('')

    const scores=[a,b,c]
        .map(p=>{
            return answers.map((answer,i)=>)
        })
}